version: "3.3"
services:
  nginx:
    image: ${REGISTRY_URL}/workspace/nginx:staging
    restart: always
    ports:
      - "0.0.0.0:${APP_PORT}:80"
  client:
    image: ${REGISTRY_URL}/workspace/client:staging
  backend:
    image: ${REGISTRY_URL}/workspace/backend:staging
    restart: on-failure
  postgres-db:
    volumes:
      - postgres:/var/lib/postgresql/data
volumes:
  postgres: ~
