type Query {
  getClipboards: [Clipboard]!
}

type Mutation {
  makeClipboard(data: ClipboardInput!): Clipboard!
  removeClipboard(id: ID!): Clipboard!
}

input ClipboardInput {
  content: String!
}

type Subscription {
  subscribeClipboards: [Clipboard]!
}
