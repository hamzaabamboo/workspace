type Query {
  getClipboards: [Clipboard]!
}

type Mutation {
  makeClipboard(data: ClipboardInput): Clipboard!
  deleteClipboard(id: ID!): Clipboard!
}

input ClipboardInput {
  content: String!
}

type Subscription {
  clipboards: [Clipboard]!
}
